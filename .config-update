#!/bin/bash

CURRENT_DIR=$(cd "$( dirname "$0" )" && pwd)
DOTFILES=$(git ls-tree -r HEAD --name-only | grep -v ".config-" | grep -v "README.md" | sed -e ':a;N;$!ba;s/\n/ /g')
cd $HOME
rsync -vlR --relative --perms ${DOTFILES} "${CURRENT_DIR}/" 2> /dev/null

